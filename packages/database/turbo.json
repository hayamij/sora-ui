{
  "extends": ["//"],
  "tasks": {
    "db:generate": {
      "env": ["DATABASE_URL", "SKIP_DB_GENERATE"]
    },
    "db:push": {
      "env": ["DATABASE_URL"]
    },
    "db:studio": {
      "env": ["DATABASE_URL"]
    }
  }
}
